query Plant($id: Int!) {
	plant(id: $id) {
        id
        title
        createdAt
        updatedAt
        waterAutomatically
        readings {
            id
            value
            createdAt
        }
    }
}